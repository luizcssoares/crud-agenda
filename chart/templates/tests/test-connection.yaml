apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "apirestcalculadora.fullname" . }}-test-connection"
  labels:
    {{- include "apirestcalculadora.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "apirestcalculadora.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
