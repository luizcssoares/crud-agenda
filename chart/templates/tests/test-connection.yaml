apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "crud-agenda.fullname" . }}-test-connection"
  labels:
    {{- include "crud-agenda.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "crud-agenda.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
