apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor

metadata:
  name: crud-agenda
  namespace: default

spec:
  selector:
    matchLabels:
      app: crud-agenda

  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s